import styled from "styled-components";

export const Wrapper = styled.section`
padding: 8rem 8rem;
.page-elements {
    width: 90rem;
    display: flex;
    flex-direction: column;
    gap: 2rem
}
.quick-links {
    display: flex;
    flex-direction: column;
    gap: 2rem
}
`